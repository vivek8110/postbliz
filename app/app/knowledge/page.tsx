import { EmptyState } from "../empty-state";

export default function KnowledgePage() {
  return (
    <EmptyState title="No facts yet">
      Point Postbliz at a source — your site, a changelog, an RSS feed — and it&apos;ll pull out what&apos;s worth posting. Every post traces back to one of these.
    </EmptyState>
  );
}
