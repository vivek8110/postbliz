import { EmptyState } from "../empty-state";

export default function ChannelsPage() {
  return (
    <EmptyState title="No channels connected">
      Connect X, LinkedIn, or Reddit and Postbliz will publish on your cadence — and keep Reddit safe.
    </EmptyState>
  );
}
